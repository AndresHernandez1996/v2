export const HERO_ANIMATION = {
  navDelayMs: 120,
  staggerMs: 100,
  itemTimeoutMs: 420,
} as const;

export const NAV_ANIMATION = {
  mountDelayMs: 120,
  staggerMs: 100,
  itemTimeoutMs: 320,
} as const;

export const ABOUT_ANIMATION = {
  mountDelayMs: 160,
  staggerMs: 120,
  itemTimeoutMs: 420,
} as const;
