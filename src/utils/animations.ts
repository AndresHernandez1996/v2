export const HERO_ANIMATION = {
  navDelayMs: 120,
  staggerMs: 100,
  itemTimeoutMs: 420,
} as const;

export const NAV_ANIMATION = {
  mountDelayMs: 120,
  staggerMs: 100,
  itemTimeoutMs: 320,
} as const;

export const ENABLE_INITIAL_LOADER = true;
