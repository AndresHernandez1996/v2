import styles from './Experience.module.scss';

export function Experience() {
  return (
    <section id="experience" className={styles.section} data-sr="text">
      <h2>Experience</h2>
      <p>
        Experience timeline will live here: role, company, period, and key
        impact.
      </p>
    </section>
  );
}
